# Write your MySQL query statement below
SELECT p.product_name, SUM(o.unit) AS unit FROM products p JOIN Orders o ON p.product_id = o.product_id AND MONTH(o.order_date) = 2 GROUP BY DATE_FORMAT(order_date,'%Y-%m'), o.product_id HAVING unit >= 100;